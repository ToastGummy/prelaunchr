<div class="hero">
  <p class="large brandon">Its almost time to Toast!</p>
  <p class="small brandon">The hangover's reign of terror is coming to an end.<br>Real science. Real results. Real soon.</p>
</div>
<div class="form-wrap clearfix <%= "ended" if Rails.application.config.ended %>">
  <% if Rails.application.config.ended %>
    <%= render "users/campaign_ended" %>
  <% else %>
    <%= render "users/campaign_ongoing" %>
  <% end %>
</div>
